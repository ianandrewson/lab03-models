// const Model = require('../lib/Model.js');
// const fsFuncs = require('../lib/fileSystemFunctions.js');

// jest.mock('fsFuncs', () => {

// });

describe('will finish later', () => {
    it('will be finished later', () => {
        expect(1).toEqual(1);
    });
});
